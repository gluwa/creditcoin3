[package]
authors.workspace = true
edition.workspace = true
license.workspace = true
name = "attestor-primitives"
publish.workspace = true
repository.workspace = true
version.workspace = true

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
# Workspace
bls-signatures = { workspace = true, default-features = false }
starknet-crypto = { workspace = true, default-features = false }
starknet-types-core = { workspace = true, default-features = false }

# Substrate
frame-support = { workspace = true, default-features = false }
parity-scale-codec = { workspace = true, default-features = false }
scale-info = { workspace = true, default-features = false }
sp-api = { workspace = true, default-features = false }
sp-core = { workspace = true, default-features = false }
sp-io = { workspace = true, default-features = false }
sp-runtime = { workspace = true, default-features = false }
sp-std = { workspace = true, default-features = false }

utils = { workspace = true, default-features = false }

ccnext-abi-encoding = { workspace = true, optional = true }
serde = { workspace = true, default-features = false, features = ["derive"] }
serde_bytes = { version = "0.11.14", default-features = false }

[dev-dependencies]
serde_json = { workspace = true, default-features = false }

[features]
default = ["std"]
std = [
    "parity-scale-codec/std",
    "frame-support/std",
    "sp-core/std",
    "sp-std/std",
    "sp-runtime/std",
    "sp-io/std",
    "serde/std",
    "serde_bytes/std",
    "scale-info/std",
    "sp-api/std",
    "bls-signatures/std",
    "utils/std",
    "starknet-types-core/std",
    "starknet-crypto/std",
    "ccnext-abi-encoding",
]
