# External Prover Example

This is basically an Flask API that is integrated with the prover binary to compute proofs based on the Public Input generated by the prover (light mode).

## Installation

```sh
python3.10 -m venv ~/flask
pip install -r requirements.txt
```

## Running

```sh
python main.py
```
