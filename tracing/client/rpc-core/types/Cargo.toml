[package]
description = "Moonbeam RPC core types (vendored from Moonbeam, maintained by Gluwa)"
edition = "2021"
license = "GPL-3.0-only"
name = "moonbeam-rpc-core-types"
version = "0.1.0"

[dependencies]
ethereum-types = { version = "0.15", features = ["std"] }
serde = { version = "1", features = ["derive"] }
