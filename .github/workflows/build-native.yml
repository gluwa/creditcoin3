# checkov:skip=CKV2_GHA_1:Caller is responsible for permissions
---
name: Reusable workflow to build a native client

"on":
  workflow_call:
    inputs:
      build-options:
        required: true
        type: string
      version-string:
        required: true
        type: string
      git-lfs-checkout:
        required: true
        type: boolean
      cache-key-name:
        required: false
        type: string
      runs-on:
        required: true
        type: string
      build-subkey:
        required: false
        type: boolean
      build-subxt:
        required: false
        type: boolean
      use-new-metadata:
        required: false
        type: boolean
      # Lets a caller run this purely to populate the Rust dependency cache, without
      # leaving ~200 MB of binaries behind that nothing downloads. Defaults to true so
      # every existing caller is unaffected.
      upload-artifacts:
        required: false
        type: boolean
        default: true

# Ubuntu 26.04 ships GCC 15, whose libstdc++ no longer transitively includes
# <cstdint>. The bundled RocksDB 8.1.1 headers (librocksdb-sys 0.11.0+8.1.1)
# use uint64_t without including it, so force-include cstdint when compiling C/C++.
env:
  CXXFLAGS: "-include cstdint"

jobs:
  build-creditcoin-node:
    runs-on: ${{ fromJSON(inputs.runs-on) }}
    steps:
      - name: Checkout code regularly
        if: ${{ ! inputs.use-new-metadata }}
        uses: actions/checkout@v7
        with:
          lfs: ${{ inputs.git-lfs-checkout }}

      - name: Checkout code for typedefs update
        if: ${{ inputs.use-new-metadata }}
        uses: actions/checkout@v7
        with:
          lfs: ${{ inputs.git-lfs-checkout }}
          repository: ${{ github.event.pull_request.head.repo.full_name }}
          ref: ${{ github.event.pull_request.head.ref }}

      - name: Install protobuf
        uses: arduino/setup-protoc@v3
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Install libclang
        run: sudo apt-get update && sudo apt-get install -y libclang-dev

      - name: Configure rustc version
        run: |
          RUSTC_VERSION=$(grep channel rust-toolchain.toml | tail -n1 | tr -d " " | cut -f2 -d'"')
          echo "RUSTC_VERSION=$RUSTC_VERSION" >> "$GITHUB_ENV"

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: ${{ env.RUSTC_VERSION }}
          targets: wasm32v1-none
          components: rust-src

      # Hosted and self-hosted runners compute the *same* Swatinem key today: same arch, same
      # rustc, same manifests, and build-native.yml sets no CXXFLAGS on either side, so the
      # env hash matches too (`...-6cf672bd-...` on both). Their Cargo home and checkout root
      # differ though, and `target/` fingerprints and `.d` files record absolute paths, so an
      # archive restored across families fails every fingerprint check and rebuilds anyway
      # after paying for the download. Give each family its own namespace, otherwise the
      # hosted warmer below wins the entry the Linode build needs and the Linode warmer can
      # never save.
      - name: Identify runner family
        id: runner-family
        env:
          RUNNER_FAMILY: ${{ runner.environment }}
        run: |
          ID=$(printf '%s\n' "${RUNNER_FAMILY:-unknown}" "$HOME" | sha256sum | cut -c1-8)
          echo "id=$ID" >> "$GITHUB_OUTPUT"

      - uses: Swatinem/rust-cache@v2
        with:
          # The default keeps callers that omit `cache-key-name` (proof-gen-api-server) in the
          # same namespace as the warmer, since they build the same configuration.
          shared-key: ${{ inputs.cache-key-name || 'build-creditcoin-node' }}-${{ steps.runner-family.outputs.id }}

      # Copied in rather than `git apply`-ed, see the producer in ci.yml's
      # regenerate-metadata. This job is the one that *must* sit on the PR branch
      # head, because it commits the result back below, so it was the consumer most
      # exposed to the checkout mismatch a diff is sensitive to.
      - name: Download regenerated metadata
        if: ${{ inputs.use-new-metadata }}
        uses: actions/download-artifact@v8
        with:
          name: new-metadata
          path: .

      - name: Show what the regenerated metadata changed
        if: ${{ inputs.use-new-metadata }}
        run: git diff --binary

      - name: Build SUT
        uses: gluwa/cargo@dev
        with:
          command: build
          args: ${{ inputs.build-options }}

      - name: Upload binaries
        if: ${{ inputs.upload-artifacts }}
        uses: actions/upload-artifact@v7
        with:
          name: binary-for-${{ inputs.version-string }}
          path: |
            target/release/archiver
            target/release/attestor
            target/release/attestor_zombienet
            target/release/checkpoint-builder
            target/release/creditcoin3-node
            target/release/proof-gen-api-server
            target/release/query-cli

      - name: Upload WASM runtime
        if: ${{ inputs.upload-artifacts }}
        uses: actions/upload-artifact@v7
        with:
          name: creditcoin_node_runtime.compact.compressed.wasm-${{ inputs.version-string }}
          path: target/release/wbuild/creditcoin3-runtime/creditcoin3_runtime.compact.compressed.wasm

      - name: Install subkey
        if: ${{ inputs.build-subkey }}
        uses: gluwa/cargo@dev
        with:
          command: install
          args: --locked --git https://github.com/paritytech/polkadot-sdk --rev 7304295 subkey

      - name: Upload subkey binary
        if: ${{ inputs.build-subkey }}
        uses: actions/upload-artifact@v7
        with:
          name: subkey
          path: ~/.cargo/bin/subkey

      - name: Install subxt
        if: ${{ inputs.build-subxt }}
        uses: gluwa/cargo@dev
        with:
          command: install
          # keep in step with the `subxt` library pin in the root Cargo.toml:
          # the CLI writes common/cc-client/artifacts/metadata.scale and the
          # library's #[subxt::subxt(...)] macro has to parse it
          args: subxt-cli@0.44.3 --locked

      - name: Upload subxt binary
        if: ${{ inputs.build-subxt }}
        uses: actions/upload-artifact@v7
        with:
          name: subxt
          path: ~/.cargo/bin/subxt

      - name: Commit typedefs and metadata changes after successfull build
        if: ${{ inputs.use-new-metadata }}
        uses: EndBug/add-and-commit@v10
        with:
          add: "cli/creditcoin.json cli/src/lib/interfaces/ common/cc-client/artifacts/metadata.scale"
          author_name: gluwa-bot
          author_email: creditcoin@gluwa.com
          message: "Auto-update metadata & TypeScript type definitions"
          github_token: ${{ secrets.GLUWA_BOT_TOKEN }}
